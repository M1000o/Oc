export type ServiceId = number;

export interface ServiceOption {
    id: ServiceId;
    label: string;
    icon: string;
}

export interface ServiceResponse {
    id: number;
    nombre: string;
  }
    